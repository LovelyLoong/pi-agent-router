# add-global-agent-supervisor

Make pi-agent-router the fail-closed global supervisor for plugin-dispatched Pi Agent, subagent, and auxiliary LLM jobs, with fresh isolated workers, structured ownership/dependencies, deterministic cleanup, compliance admission, and migrated controlled callers.
